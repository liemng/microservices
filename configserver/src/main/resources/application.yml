server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/liemng/microconfig.git
          searchPaths: subscriptionservice,billingservice,zuulserver